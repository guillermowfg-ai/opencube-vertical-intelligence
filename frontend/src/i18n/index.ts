/** Everything a screen needs from the language layer. The provider itself is
 * imported directly from `./I18nProvider` by the app entry point. */

export { useI18n, fill, type Language, type I18nValue } from "./context";
export { type Dictionary } from "./en";
